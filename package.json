{
  "name": "audio-transfer",
  "version": "1.0.0",
  "description": "音频格式转换程序",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager ./ audioFormat  --platform=win32 --arch=x64 --out ./outApp --overwrite"
  },
  "keywords": [
    "Electron",
    "ffmpeg",
    "audio",
    "transfer"
  ],
  "author": "GitHub",
  "license": "CC0-1.0",
  "devDependencies": {
    "electron": "^3.0.6",
    "electron-packager": "^12.2.0"
  },
  "dependencies": {
    "ffmpeg-binaries": "^4.0.0",
    "fluent-ffmpeg": "^2.1.2"
  }
}
